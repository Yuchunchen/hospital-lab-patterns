# Real patient data — NEVER commit
examples/
*.real.html
*.real.json
*.real.txt
*-chartno-*.html
*-chartno-*.json

# Common
node_modules/
.DS_Store
Thumbs.db
*.log
.env
.env.*

# Editors
.vscode/
.idea/
*.swp
*.tmp.*

# Build artefacts
# dist/patterns.json is COMMITTED — that's how OPD extensions auto-update.
# Other build outputs (if any) are still ignored.
build/
